import Spinner from '@/components/spinner/Spinner';
import React from 'react';

const loading = () => {
    return (
        <div>
            <Spinner/>
        </div>
    );
};

export default loading;